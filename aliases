# ls
alias ls='ls --color=auto -h --group-directories-first'
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# grep
alias grep='grep --color=auto'
alias fgrep='fgrep --color=auto'
alias egrep='egrep --color=auto'

# flexget
# alias "flexget"="~/Flexget/bin/flexget --logfile ~/Flexget/flexget.log"
alias "flexget-cron"="/usr/bin/time -o /home/media/.flexget/flexget.log -a -f 'Run time was %E' /usr/bin/flexget execute -c /home/media/.flexget/config.yml --cron"

# SortTV config shortcuts (Need to verify these before enabling them for real)
# alias sort-tv-shows='perl ~/sorttv/sorttv.pl --read-config-file=~/sorttv/sorttv.shows'
# alias sort-tv-premiers='perl ~/sorttv/sorttv.pl --read-config-file=~/sorttv/sorttv.premiers'
# alias sort-movies='perl ~/sorttv/sorttv.pl --read-config-file=~/sorttv/sorttv.movie'

# git
alias gdiff='git diff --no-index'
alias gs='git status'

# todo.txt
alias t='~/.todo/todo.sh -d ~/.todo/todo.cfg'
alias ta='t a'
alias tn='pri A'
alias tw='t view project @waiting && echo "=====  Stalled   =====" && t view project @next @waiting'
alias tah='t lsp A | grep "+home"'
alias tas='t lsp A | grep "@shopping"'
alias tshop='t view project @shopping'
alias tprog='t view project @inprogress'
alias tbi='t bi'
alias context='t view context'
alias pri='t lsp'
alias tmkol='t ol mkol'

# ghi GitHub issue tracker cli
alias gi='$HOME/bin/ghi --no-pager'

# csvfix
alias readtab='csvfix read_dsv -s "\t"'

# other
alias speed_test='wget -O/dev/null speedtest.pixelwolf.ch'
alias freq='cut -f1 -d" " ~/.bash_history | sort | uniq -c | sort -nr | head -n 30'

alias plot_trans='find_and_plot /cygdrive/f/Dyno/Trans'

alias pycheck='find -name "*.py" -not -path "*venv*" -exec flake8 {} \;'

alias mkdir='mkdir -p'

# legder
alias ledger='ledger "-f C:\Users\cgiacofe\Dropbox\Synced\finances\ledgerindex.ledger"'
export finjrn=$HOME'/Dropbox/Synced/finances/finances.ledger'
export finfold=$HOME'/Dropbox/Synced/finances/'
